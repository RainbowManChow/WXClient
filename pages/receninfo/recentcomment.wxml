  <view class="userInfo">
      <image class='user-head' src='{{userInfo.avatarUrl}}'/>
      <view class='user-name'>{{userInfo.nickName}}</view>
  </view>
 <view class="touch-item {{item.isTouchMove ? 'touch-move-active' : ''}}" data-index="{{index}}" bindtouchstart="touchstart" bindtouchmove="touchmove" wx:for="{{items}}" wx:key="" bindtap="godetail" data-id="{{item.markerid}}" >
  <view class="content"><image style="height:3vw;width:3vw" src='/images/arrow-right.png'></image>{{item.txt}}
  <span style="position:absolute;right: 4vw;">{{item.needrecentdate}}</span>
  </view>
  <view class="del" catchtap="del" data-index="{{index}}" data-id="{{item.id}}">删除</view>
 </view>