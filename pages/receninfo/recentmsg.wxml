  <view class="userInfo">
      <image class='user-head' src='{{userInfo.avatarUrl}}'/>
      <view class='user-name'>{{userInfo.nickName}}</view>
  </view>
 <view class="touch-item {{item.isTouchMove ? 'touch-move-active' : ''}}" data-index="{{index}}" bindtouchstart="touchstart" bindtouchmove="touchmove" wx:for="{{items}}" wx:key="" bindtap="godetail" data-id="{{item.id}}"  data-info="{{item.info}}" data-status="{{item.status}}"  data-title="{{item.replaytitle}}" data-userName="{{item.userName}}" data-userPhoto="{{item.userPhoto}}"  data-needrecentdate="{{item.needrecentdate}}" data-helpid="{{item.helpid}}" >
  <view class="content"><image style="height:3vw;width:3vw" src='/images/arrow-right.png'></image>{{item.title}}
  <span style="position:absolute;right: 4vw;color:red" wx:if="{{item.status==0}}" >未读</span>
  <span style="position:absolute;right: 20vw;">{{item.needrecentdate}}</span>
  </view>
  <view class="del" catchtap="del" data-index="{{index}}" data-id="{{item.id}}">删除</view>
 </view>
